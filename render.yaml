services:
  - type: web
    name: manhole-dashboard
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm install -g serve && serve -s build -l 3000
    envVars:
      - key: NODE_ENV
        value: production
